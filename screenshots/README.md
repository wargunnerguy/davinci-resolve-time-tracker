# Screenshots

Drop screenshot images here so they show up in the main README:

- `main.png` — the main tracker window
- `mini.png` — the compact mini pill
- `stats.png` — the Stats / Export window

PNG is recommended. Keep them a reasonable width (~600–900px) so the README
renders nicely.
